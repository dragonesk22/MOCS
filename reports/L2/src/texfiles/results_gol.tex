\newpage
\subsection{Simulation Results}
\label{golres}

\subsubsection{NOT-gate realisation}
\subsubsection*{Initial conditions}
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figs/GoL/NOT_IC}
  \caption{Two Gosper glider guns: the left gun provides a constant input stream, while the input channel injects gliders traveling in the south-east direction. No input is encoded by inserting an eater in the SE input stream. The output is read as gliders propagating in the south-west direction.}
  \label{fig:NOT_0}
\end{figure}

\subsubsection*{Transient state}
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figs/GoL/NOT_488.pdf}
  \caption{One input channel, $A$. The input glider moves diagonally from the top to the lower-right part of the grid. The output is observed after the interaction region, in the outgoing diagonal channel. The construction is arranged so that when $A=0$ an output glider survives, while when $A=1$ the input glider destroys or blocks the outgoing signal.}
  \label{fig:NOT}
\end{figure}

\subsubsection{AND-gate realisation}

\subsubsection*{Initial conditions}
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figs/GoL/AND_IC}
  \caption{Three Gosper glider guns: the two leftmost generate south-east-directed input streams. An eater suppresses the south-west output unless both inputs
   are present. In the absence of inputs, additional eaters are placed along the collision paths to block spurious gliders. The output is read as a
   south-east-directed stream.}
  \label{fig:AND_0}
\end{figure}

\subsubsection*{Transient state}
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figs/GoL/AND_464.pdf}
  \caption{Two input channels are used, $A$ and $B$. The input gliders move diagonally from the top of the grid towards the lower-right part of the grid, where they interact with the gate structure. The output is observed in the outgoing diagonal channel after this interaction. A glider appears in the output channel only when both input gliders are
 present, so the output is $1$ only for $A=1$ and $B=1$.}
  \label{fig:AND}

\end{figure}

\subsubsection{OR-gate realisation}

\subsubsection*{Initial conditions}
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figs/GoL/OR_IC}
  \caption{Four Gosper glider guns: the second and third generate south-east-directed input streams. Logical zeros are implemented by placing eaters along the input collision paths. The output is read as a south-east-directed stream.}
  \label{fig:OR_0}
\end{figure}

\subsubsection*{Transient state}
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figs/GoL/OR_465.pdf}
  \caption{Input gliders also move diagonally from the top of the grid towards the lower-right part of the grid. The output is shown in the outgoing diagonal channel after the region of collision. In this case, the output channel contains a glider when, at least, one of the two input gliders is present.}
  \label{fig:OR}
\end{figure}

In the snapshots above, the realization of logic gates within a computer simulation of the
Game of Life is presented. The validity of the implementation of \texttt{AND} and \texttt{OR} logic gates (see Figures
(\ref{fig:AND} \& \ref{fig:OR})) can be verified succinctly: both produce a south-east directed output stream that is
consistent with their respective truth tables. The \texttt{NOT}-gate has a south-west directed output stream, that
negates input signals via collision (in Figure (\ref{fig:NOT}) the gliders are in a collision course where the
mutually annihilate each other).

From this, one may conclude that a Boolean basis is effectively implemented within the system.
Once the principles for composing these gates into larger circuits are understood, increasingly complex computational structures can be constructed.
The existence of logic gates within the Game of Life implies that they form a functionally complete basis for Boolean logic.
Consequently, any Boolean function can be expressed as a finite composition of such gates. This property is fundamental for the construction of general
 computational devices. In particular, it is possible to design patterns that behave as a
 \href{https://en.wikipedia.org/wiki/Finite-state_machine}{finite-state machine} coupled to two counters.
Such a system is computationally equivalent to a
\href{https://en.wikipedia.org/wiki/Universal_Turing_machine}{universal Turing machine}.

